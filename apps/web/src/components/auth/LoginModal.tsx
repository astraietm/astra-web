"use client";

import React from "react";

// The authentication flow is now managed on the dedicated full-page /login route.
const LoginModal: React.FC = () => {
  return null;
};

export default LoginModal;

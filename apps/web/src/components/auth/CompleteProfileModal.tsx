'use client';

// Deprecated overlay: Profile completion and editing is now managed directly on the dedicated /profile page.
const CompleteProfileModal: React.FC = () => {
  return null;
};

export default CompleteProfileModal;
